{
  "connection_settings": {
    "description": "连接设置：远端 grok2api 地址、Client Key、TLS 与代理。",
    "type": "object",
    "items": {
      "enabled": {
        "description": "总开关。关闭后插件不响应任何命令，也不注册/暴露搜索 Tool。",
        "type": "bool",
        "default": true,
        "hint": "关闭后命令与自动搜索均不可用。"
      },
      "api_base_url": {
        "description": "远端 grok2api 基础地址",
        "type": "string",
        "default": "",
        "obvious_hint": true,
        "hint": "填写 https://grok.example.com 这类可由 AstrBot 访问的远端地址；不要附加 /v1。"
      },
      "client_api_key": {
        "description": "grok2api Client Key。仅用于鉴权，绝不写入日志。",
        "type": "string",
        "default": "",
        "hint": "只使用 Client Key，不要填写管理员 JWT、账号 SSO/OAuth 或 QQ AppID/AppSecret。"
      },
      "verify_tls": {
        "description": "是否校验证书。生产环境请保持开启。",
        "type": "bool",
        "default": true,
        "hint": "仅本地自签名或内部 CA 场景可考虑关闭。"
      },
      "client_proxy_url": {
        "description": "AstrBot 到 grok2api 的 HTTP(S) 代理。首版只允许 http/https。",
        "type": "string",
        "default": "",
        "hint": "需要认证时可写 http://user:pass@host:port；日志与状态页只显示协议/主机/端口。示例 http://proxy.example:8080。"
      },
      "admin_username": {
        "description": "grok2api 管理面登录用户名。仅用于 /g2面板 只读查询，绝不写入日志。",
        "type": "string",
        "default": "",
        "hint": "仅 bot 主人接线。泄露即有权读取上游账号与聚合数据。"
      },
      "admin_password": {
        "description": "grok2api 管理面登录密码。仅用于 /g2面板 只读查询，绝不写入日志。",
        "type": "string",
        "default": "",
        "hint": "泄露即有权读取上游账号与聚合数据；务必只由 bot 主人填写。"
      }
    }
  },
  "capability_settings": {
    "description": "能力设置：搜索模型候选、图片/视频模型与发送行为。",
    "type": "object",
    "items": {
      "search_models": {
        "description": "联网搜索模型候选，英文逗号分隔，左侧优先，最多 12 个。留空则搜索能力不可用。",
        "type": "string",
        "default": "grok-4.5,grok-4.3,grok-4.20-0309-reasoning,grok-4.20-0309-non-reasoning,grok-4.20-multi-agent-0309,grok-build-0.1,grok-chat-fast",
        "hint": "使用英文逗号 , 分隔，按首次出现保序去重。左侧模型优先；远端失败先按重试设置重试，只有 model_not_found/model_not_allowed/search_not_performed 耗尽后才切换下一个。"
      },
      "enable_web_search": {
        "description": "启用 Web 联网搜索。",
        "type": "bool",
        "default": true,
        "hint": "默认开启；关闭后仅使用已启用的 X 搜索。"
      },
      "enable_x_search": {
        "description": "启用 X 搜索。",
        "type": "bool",
        "default": true,
        "hint": "默认开启；grok-chat-* 模型不支持 X 搜索，会自动仅使用已启用的 Web 联网搜索。"
      },
      "search_reasoning_effort": {
        "description": "联网搜索思考强度。",
        "type": "string",
        "default": "high",
        "options": ["auto", "none", "low", "medium", "high", "xhigh"],
        "hint": "默认 high；auto 表示不发送 reasoning 参数，由远端自动选择。当前候选模型不支持所选强度时也会省略该参数。"
      },
      "image_model": {
        "description": "文生图模型。留空则生图不可用。",
        "type": "string",
        "default": "",
        "hint": "例如 grok-imagine-image。"
      },
      "image_edit_model": {
        "description": "改图模型。留空则改图不可用。",
        "type": "string",
        "default": "",
        "hint": "例如 grok-imagine-image。"
      },
      "video_model": {
        "description": "生视频模型。留空则视频不可用。",
        "type": "string",
        "default": "",
        "hint": "例如 grok-imagine-video。"
      },
      "enable_llm_search_tool": {
        "description": "会话级注册联网搜索 Tool。",
        "type": "bool",
        "default": true,
        "hint": "主模型按 Tool 描述决定是否自动搜索。关闭后仅手动 /g2搜索 可用。"
      },
      "show_search_sources": {
        "description": "搜索结果是否展示结构化来源。",
        "type": "bool",
        "default": true,
        "hint": "手动命令输出与 Tool 返回内容均遵守此开关。"
      },
      "max_search_sources": {
        "description": "单个搜索结果最多展示的来源数。",
        "type": "int",
        "default": 5,
        "slider": { "min": 0, "max": 10, "step": 1 },
        "hint": "0 表示不展示来源。"
      },
      "max_search_output_chars": {
        "description": "搜索结果正文按 Unicode 字符截断的上限。",
        "type": "int",
        "default": 6000,
        "slider": { "min": 500, "max": 20000, "step": 100 },
        "hint": "超出后追加 [内容已截断] 标记。"
      },
      "image_response_format": {
        "description": "生图/改图响应格式。",
        "type": "string",
        "default": "b64_json",
        "options": ["b64_json", "url"],
        "hint": "无论哪种都会先落盘再发送。"
      },
      "prompt_processing": {
        "description": "提示词处理：关闭时完整直传；整理仅补全媒体参数；优化会重写提示词并补全参数。",
        "type": "object",
        "items": {
          "mode": {
            "description": "提示词处理模式。",
            "type": "string",
            "default": "off",
            "options": ["off", "extract", "enhance"],
            "hint": "off=原文直传；extract=仅提取时长、比例、分辨率；enhance=优化提示词与参数。"
          },
          "extract_provider_id": {
            "description": "提示词整理模型。",
            "type": "string",
            "default": "",
            "_special": "select_provider",
            "hint": "仅在整理模式使用；从 AstrBot 已配置的文本供应商中选择。"
          },
          "enhance_provider_id": {
            "description": "提示词优化模型。",
            "type": "string",
            "default": "",
            "_special": "select_provider",
            "hint": "仅在优化模式使用；可与整理模型选择不同供应商。"
          }
        }
      },
      "send_media_progress": {
        "description": "媒体生成前发送一次进度提示。",
        "type": "bool",
        "default": true,
        "hint": "覆盖生图、改图和视频；QQ Official 建议开启且每个任务只发一次。"
      }
    }
  },
  "access_settings": {
    "description": "访问控制：用户与群的黑/白名单。",
    "type": "object",
    "items": {
      "user_whitelist": {
        "description": "允许使用本插件能力的用户 ID 白名单，空表示不限制。",
        "type": "list",
        "default": []
      },
      "user_blacklist": {
        "description": "禁止使用本插件能力的用户 ID 黑名单，优先于白名单。",
        "type": "list",
        "default": []
      },
      "group_whitelist": {
        "description": "允许使用本插件能力的群聊 ID 白名单，仅群聊生效，空表示不限制。",
        "type": "list",
        "default": []
      },
      "group_blacklist": {
        "description": "禁止使用本插件能力的群聊 ID 黑名单，优先于白名单。",
        "type": "list",
        "default": []
      }
    }
  },
  "advanced_settings": {
    "description": "高级设置：超时、并发、媒体大小与日志。",
    "type": "object",
    "items": {
      "connect_timeout_seconds": {
        "description": "TCP 连接超时（秒）。",
        "type": "int",
        "default": 10,
        "slider": { "min": 1, "max": 60, "step": 1 }
      },
      "search_timeout_seconds": {
        "description": "搜索请求整体超时（秒）。",
        "type": "int",
        "default": 180,
        "slider": { "min": 10, "max": 600, "step": 10 }
      },
      "image_timeout_seconds": {
        "description": "生图/改图请求整体超时（秒）。",
        "type": "int",
        "default": 300,
        "slider": { "min": 30, "max": 900, "step": 30 }
      },
      "video_create_timeout_seconds": {
        "description": "视频创建请求整体超时（秒）。",
        "type": "int",
        "default": 120,
        "slider": { "min": 10, "max": 600, "step": 10 }
      },
      "video_poll_timeout_seconds": {
        "description": "每次视频状态查询请求整体超时（秒）。",
        "type": "int",
        "default": 30,
        "slider": { "min": 1, "max": 600, "step": 1 }
      },
      "video_poll_interval_seconds": {
        "description": "视频状态轮询间隔（秒）。",
        "type": "int",
        "default": 3,
        "slider": { "min": 1, "max": 30, "step": 1 }
      },
      "download_timeout_seconds": {
        "description": "媒体下载整体超时（秒）。",
        "type": "int",
        "default": 300,
        "slider": { "min": 30, "max": 1800, "step": 30 }
      },
      "prompt_processing_timeout_seconds": {
        "description": "提示词处理模型超时（秒）。",
        "type": "int",
        "default": 15,
        "slider": { "min": 1, "max": 60, "step": 1 },
        "hint": "整理或优化模型超时、调用失败或返回非预期 JSON 时，本次媒体生成会终止。"
      },
      "max_input_image_mb": {
        "description": "改图输入图片最大体积（MiB）。",
        "type": "int",
        "default": 12,
        "slider": { "min": 1, "max": 24, "step": 1 },
        "hint": "为默认 32 MiB JSON 请求体预留 Base64 膨胀空间。"
      },
      "max_image_download_mb": {
        "description": "生图结果下载最大体积（MiB）。",
        "type": "int",
        "default": 25,
        "slider": { "min": 1, "max": 100, "step": 1 }
      },
      "max_video_download_mb": {
        "description": "视频下载最大体积（MiB）。",
        "type": "int",
        "default": 190,
        "slider": { "min": 1, "max": 200, "step": 1 },
        "hint": "低于 QQ Official 200 MiB 硬上限。"
      },
      "max_concurrent_searches": {
        "description": "全局并发搜索上限。",
        "type": "int",
        "default": 4,
        "slider": { "min": 1, "max": 16, "step": 1 }
      },
      "max_concurrent_media_jobs": {
        "description": "全局并发生图/改图/视频任务上限。",
        "type": "int",
        "default": 2,
        "slider": { "min": 1, "max": 8, "step": 1 }
      },
      "model_retry_count": {
        "description": "搜索、生图、改图、模型目录和图片下载的最大额外重试次数；不含首次请求。",
        "type": "int",
        "default": 2,
        "slider": { "min": 0, "max": 5, "step": 1 }
      },
      "video_retry_count": {
        "description": "视频创建、状态轮询和视频下载的最大额外重试次数；不含首次请求。",
        "type": "int",
        "default": 2,
        "slider": { "min": 0, "max": 5, "step": 1 }
      },
      "retry_base_delay_seconds": {
        "description": "重试退避基准延迟（秒）。",
        "type": "float",
        "default": 0.5,
        "slider": { "min": 0.1, "max": 5.0, "step": 0.1 }
      },
      "retry_excluded_errors": {
        "description": "不自动重试的错误，英文逗号分隔；可填 HTTP 状态码或稳定错误码，留空即全部远端错误重试。",
        "type": "string",
        "default": "",
        "hint": "示例：400,401,403,404,422,auth_error,model_not_found,invalid_json,network_error"
      },
      "save_media": {
        "description": "是否在发送后保留生成媒体。",
        "type": "bool",
        "default": false,
        "hint": "false 发送后删除；true 成功文件移到 archive/ 保留。"
      },
      "temp_retention_hours": {
        "description": "启动时清理超过该时长的未归档临时媒体。",
        "type": "int",
        "default": 24,
        "slider": { "min": 1, "max": 168, "step": 1 }
      },
      "panel_period": {
        "description": "/g2面板 使用的统计周期，同时作用于请求审计汇总与按模型统计。",
        "type": "string",
        "default": "7d",
        "options": ["24h", "7d", "30d", "90d"],
        "hint": "默认 7d；切换后需重载插件配置生效。"
      },
      "panel_sections": {
        "description": "/g2面板 拉取并发送的数据块（多选）。空则表示已禁用面板数据。",
        "type": "list",
        "default": ["账号池", "图片库", "视频库", "请求审计汇总", "按模型统计"],
        "options": ["账号池", "图片库", "视频库", "请求审计汇总", "按模型统计"],
        "hint": "取消勾选的块不会被拉取；全部取消后 /g2面板 将提示未启用数据块且不发起管理请求。"
      },
      "panel_t2i_enabled": {
        "description": "将 /g2面板 渲染为图片；关闭后改为纯文本发送。",
        "type": "bool",
        "default": true
      },
      "panel_resolution": {
        "description": "/g2面板 图片分辨率。",
        "type": "string",
        "default": "1080p",
        "options": ["720p", "1080p", "1440p"],
        "hint": "1080p 兼顾清晰度与文件大小；1440p 更清晰但生成和发送开销更高。"
      },
      "panel_background_tags": {
        "description": "Lolicon 背景图标签，每行一个；留空随机取图。每次面板发送随机选择一行。",
        "type": "text",
        "default": "",
        "hint": "仅请求 R18=0、排除 AI、横向图片；获取失败时复用本地缓存背景。"
      },
      "panel_push_targets": {
        "description": "固定推送目标。填写 AstrBot UMO（platform:message_type:session_id）；与命令订阅目标合并去重。",
        "type": "template_list",
        "templates": {
          "umo_target": {
            "name": "固定 UMO 目标",
            "items": {
              "umo": {
                "description": "目标 UMO",
                "type": "string",
                "default": ""
              },
              "enabled": {
                "description": "启用",
                "type": "bool",
                "default": true
              }
            }
          }
        },
        "default": []
      },
      "panel_cron_enabled": {
        "description": "启用 /g2面板 的 Cron 定时推送。",
        "type": "bool",
        "default": false
      },
      "panel_cron_expression": {
        "description": "五段 Cron 表达式：分 时 日 月 周。",
        "type": "string",
        "default": "0 9 * * *",
        "hint": "例如 0 9 * * * 表示每天 09:00。"
      },
      "panel_interval_enabled": {
        "description": "启用从本地 00:00 起对齐的间隔推送。",
        "type": "bool",
        "default": false
      },
      "panel_interval_minutes": {
        "description": "间隔推送分钟数，从每日 00:00 对齐计算。",
        "type": "int",
        "default": 30,
        "slider": { "min": 1, "max": 1440, "step": 1 }
      }
    }
  }
}
